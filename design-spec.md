# このへんにいるよ — デザイン・機能仕様書

家族や友人と買い物中に別行動になったとき、お互いのだいたいの位置がわかるアプリ。
「精密な地図」ではなく「ざっくりした方向と距離」を見せる、という前提が全体の設計思想。

このドキュメントは Claude Code に渡して実装してもらうための仕様書です。
添付の `hagure-navi.html`（機能プロトタイプ）と `color-specimen-pastel.html`（配色の最終確認用）も参照してください。

---

## 1. コンセプト

- 対象: 買い物中に別行動する家族・友人グループ
- やること: 4文字のグループコードを作成/共有し、参加した全員のだいたいの位置をリアルタイムに近い形で見る
- やらないこと: 正確なナビゲーション、ルート案内、屋内の詳細な地図表示
- 見せ方: 実際の地図ではなく「自分を中心にしたレーダー」— 方角（上=北）と距離のみを表示。屋内モールではGPSが不安定なので、精密さを装わない方が誠実、という判断
- トーン: 重いテーマ（迷子・不安）を深刻に扱いすぎない。きれいごとより正直さ。「わからない」をそのまま出す（例: 更新が止まっているメンバーは薄く表示し、隠さない）

---

## 2. 名前

**このへんにいるよ**

候補として「はぐれナビ」も検討したが、"ナビ"という語が精密な案内を期待させてしまうため却下。今の名前は、レーダー表示の"ざっくりさ"と矛盾しない、話しかけるようなトーン。

---

## 3. 主要な画面・フロー

### 3.1 セットアップ画面
- 名前入力（グループ内の表示名、最大10文字程度）
- 「新しいグループを作る」→ 4文字のランダムコード発行（紛らわしい文字は除外: `ABCDEFGHJKLMNPQRSTUVWXYZ23456789` のような文字セット）
- 「コードで参加する」→ 4文字コード入力欄
- 位置情報共有の注意書きを明示（グループコードを知っている人には位置が見える、という点を隠さず伝える）

### 3.2 メイン画面
- 上部: グループコードのチップ表示＋コピー機能、「グループを抜ける」リンク
- 中央: レーダー表示（後述）
- 下部: メンバーリスト（アバター・名前・最終更新時刻・距離）
- ステータス表示: 位置情報取得中/取得済みの状態

### 3.3 レーダー表示
- 自分を中心（center dot）に、他のメンバーを方角（北=上）と距離に応じて円周上に配置
- 距離に応じて自動スケーリングする同心円（3本、破線）。円ごとに距離ラベルを表示
- 各メンバーは自分の色のドット＋名前の頭文字1文字
- 5分以上更新がないメンバーは薄く表示（`opacity: 0.45` 程度）し、リストにも「更新が止まっています」の一言を添える

---

## 4. デザイントークン

### 4.1 タイポグラフィ
| 用途 | フォント | ウェイト | 備考 |
|---|---|---|---|
| 見出し（アプリ名など） | Zen Kaku Gothic New | 900 | 証書っぽい格式感を避けるため、当初のセリフ体（Shippori Mincho）から変更した経緯あり |
| 本文・UI全般 | Zen Kaku Gothic New | 400 / 500 / 700 | |
| 距離・時間などの数値 | Caveat（欧文専用の手書き風フォント） | 500 / 700 | 日本語グリフは無いので、数字・英数字にのみ使用 |
| グループコード | JetBrains Mono | 400 / 600 | デジタルで即物的な質感を出すための唯一のモノスペース |

Google Fonts import:
```
https://fonts.googleapis.com/css2?family=Caveat:wght@500;700&family=JetBrains+Mono:wght@400;600&family=Zen+Kaku+Gothic+New:wght@400;500;700;900&display=swap
```

### 4.2 カラー

**ベース**
| 役割 | 色 |
|---|---|
| 背景（アプリ全体） | `#EAE4D6`（温かみのあるオフホワイト／生成り） |
| カード・パネル背景 | `#F7F4EE` |
| インク（本文・枠線・アイコン） | `#4A4A48`（濃いグレー。黒 `#1D1B2A` 系から変更した経緯あり、黒だと強すぎるため） |
| サブテキスト | `#7C7A6C` |
| 罫線（破線・区切り） | `#D9D2BE` |

**メンバーカラー（1人に1色を固定割り当て）**
実際のブランドカラー案として指定された6色パレットをそのまま採用。彩度が高めのパステル系で統一。

| 役割 | 色 | 文字色（初期字などに重ねる場合） |
|---|---|---|
| 自分（あなた） | `#F8A4A4`（ピンク） | `#4A4A48`（濃いグレー） |
| メンバー1人目 | `#C5E063`（イエローグリーン） | `#4A4A48` |
| メンバー2人目 | `#8FC2DA`（ライトブルー） | `#4A4A48` |
| メンバー3人目 | `#AC91DD`（ライトパープル） | `#FFFFFF`（中間の濃さのため白抜き） |
| メンバー4人目 | `#54B65C`（グリーン） | `#FFFFFF` |
| メンバー5人目以降 | `#E5E785`（淡いイエロー） | `#4A4A48` |

割り当てルール: グループに参加した順に上から割り当てる。6人目以降は先頭に戻ってループ、またはハッシュ関数で色を決定してもよい。

**CTA（主要ボタン）**
- 背景: `#F8A4A4`（自分カラーと同じピンクで統一）
- 文字色: `#4A4A48`
- 枠線: `2.5px solid #4A4A48`（後述の「丸い要素と同じ罫」ルール参照）

### 4.3 コンポーネントのディテール

- **丸い要素（アバター、レーダー上のドット、色見本）はすべて `2〜2.5px solid #4A4A48` の枠線で統一する。**
- 主要ボタン（CTA）にも、丸い要素と揃えるために同じ `2.5px solid #4A4A48` の枠線を付ける（背景色があっても輪郭は必ずこのグレーで縁取る、というのがこのアプリの一貫したルール）。
- グループコードのチップ（バッジ）: 白背景 + `2px solid #4A4A48` + `border-radius: 999px`（ピル形状）
- 角丸: カード類は `border-radius: 14〜16px`、ボタン・チップはピル or `10px`

---

## 5. 機能要件（実装のコア）

### 5.1 グループ管理
- 4文字のランダムコードで「グループ」を識別する。バックエンドに `groups` テーブル（またはコレクション）を作り、コードをキーにする
- 参加者ごとに「名前」を紐づける（認証なしの軽量な仕組みでよい。名前の重複チェックは必須ではない）

### 5.2 位置情報
- `navigator.geolocation.watchPosition` で継続的に自分の位置（緯度経度）を取得
- 取得した位置は都度サーバーに送るのではなく、8〜10秒間隔などで間引いて送信（電池・通信量の節約）
- サーバー側は「グループコード＋名前」をキーに、最新の緯度経度とタイムスタンプを保持
- 他メンバーの位置は3〜5秒間隔でポーリング、またはWebSocket/Realtime購読で取得

### 5.3 距離・方角の計算（クライアント側）
- 2点間の距離: Haversine公式（メートル単位）
- 2点間の方角(bearing): 通常の球面三角法の公式（0〜360度、北を0とする）
- レーダー上の座標変換: `x = cx + scaled * sin(bearingRad)`, `y = cy - scaled * cos(bearingRad)`（北=上）

### 5.4 表示ロジック
- 現在地からの距離に応じて、レーダーの同心円スケールを動的に決定（最大距離 × 1.2〜1.25倍を全体の半径とする）
- 距離表示は1000m未満は10m単位に丸めて「◯◯m」、1000m以上は「◯.◯km」
- 最終更新から5分以上経過したメンバーは「古い」扱いにして視覚的に薄くする

---

## 6. プロトタイプからの引き継ぎ事項・注意点

添付の `hagure-navi.html` は Claude.ai のアーティファクト環境専用のAPI（`window.storage`）を使った機能検証用プロトタイプです。実装（本番アプリ）では以下に置き換える必要があります。

| プロトタイプでの仮実装 | 本番での置き換え候補 |
|---|---|
| `window.storage`（アーティファクト専用の共有ストレージ） | Supabase / Firebase など、実際のリアルタイムDB |
| リロードで名前・コードが消える（ブラウザストレージ不可の制約） | `localStorage` またはログインなしの軽量セッション管理で保持 |
| ポーリングのみ（4秒間隔） | 可能ならRealtime購読（Supabase Realtime, Firestore onSnapshotなど）に置き換えて即時性を上げる |
| `file://` では位置情報の許可ダイアログが出ない | 本番はHTTPS必須（Vercelなどでのホスティングを想定） |

## 7. 今後の検討事項（このドキュメントの範囲外）
- 招待の共有方法（URLのみ／QRコード／SMSなど）
- グループの自動解散タイミング（例: 全員の更新が一定時間止まったら）
- 位置情報のプライバシー説明・同意フローの具体的な文言
- オフライン・電波不良時の挙動
